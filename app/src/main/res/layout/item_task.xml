<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.graphics.Color" />

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.mayburger.dzikirqu.ui.adapters.viewmodels.ItemTaskViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:background="@drawable/shp_white_8dp"
        android:elevation="2dp"
        android:orientation="vertical"
        android:padding="14dp"
        tools:visibility="visible">

        <ImageView
            android:id="@+id/task_image"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_margin="2dp"
            android:background="@drawable/circle_white"
            android:elevation="2dp"
            android:padding="4dp"
            android:src="@{viewModel.data.totalTask == viewModel.data.taskCount ? @drawable/ic_check_primary : @drawable/circle_white}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <com.mayburger.dzikirqu.util.textfont.TextFont
            android:id="@+id/task_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="@{viewModel.title}"
            android:textColor="@color/colorBlack"
            android:textSize="16sp"
            app:layout_constraintBottom_toTopOf="@id/task_desc"
            app:layout_constraintLeft_toRightOf="@id/task_image"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Read Evening Dzikir" />

        <com.mayburger.dzikirqu.util.textfont.TextFont
            android:id="@+id/task_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{viewModel.desc}"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="@id/task_title"
            app:layout_constraintTop_toBottomOf="@id/task_title"
            tools:text="1 of 5" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>